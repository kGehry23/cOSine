# cOSine

## Booting cOSine

The kernel implemented here uses the prebuilt GRUB bootloader. 

To boot the minimal kernel, ensure that qemu is accessible from the command line. Once complete, with bash run

    ./boot_os.sh

Once executed, a qemu window should open and "Booted into cOSine" is displayed on screen.


