# cOSine

## Booting cOSine

To boot the minimal bootloader, ensure that qemu is accessible from the command line. Once complete, with bash run

    ./boot.sh

Once executed, a qemu window should open and "Booted into cOSine" is displayed on screen.

